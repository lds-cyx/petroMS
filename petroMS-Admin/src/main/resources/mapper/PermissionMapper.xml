<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lds.admin.mapper.PermissionMapper">

    <select id="selectPermission" resultType="com.lds.admin.pojo.po.PermitDetail" parameterType="java.lang.Long">
        select ur.user_id, ur.role_id, r.description, p.permit
        from petroMS.user u,
             petroMS.role r,
             petroMS.user_role ur,
             petroMS.permission p
        where #{id} = ur.user_id
          and ur.role_id = r.id
          and ur.role_id = p.role_id
    </select>
</mapper>
